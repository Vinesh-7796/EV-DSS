<!-- template_name: maintenance, template_version: 1.0.0, description: Scheduled maintenance and inspection procedures, supported_intents: maintenance -->
{{ system_prompt }}

{{ engineering_rules }}

{% if json_mode_instruction %}{{ json_mode_instruction }}

{% endif %}## Technical Context

{{ context }}

{% if conversation_history %}
## Conversation History

{{ conversation_history }}

{% endif %}
## Task

A technician is performing scheduled maintenance or inspection.
Using ONLY the context above, answer their question.

Technician query: {{ query }}

## Output Format

You MUST respond with valid JSON ONLY — no markdown, no explanations outside
the JSON structure.  Follow this exact schema:

{{ diagnostic_response_schema }}

Ensure:
- "problem_summary" describes the maintenance task or interval
- "possible_causes" lists conditions that may require maintenance
- "inspection_steps" are the inspection/check procedures
- "recommended_actions" include part numbers, fluids, torque specs, intervals
- "referenced_entities" includes components, fluids, and specifications
- "citations" maps each claim to a source
