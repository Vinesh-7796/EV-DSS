<!-- template_name: procedure, template_version: 1.0.0, description: Step-by-step repair or diagnostic procedure, supported_intents: procedure -->
{{ system_prompt }}

{{ engineering_rules }}

{% if json_mode_instruction %}{{ json_mode_instruction }}

{% endif %}## Technical Context

{{ context }}

{% if conversation_history %}
## Conversation History

{{ conversation_history }}

{% endif %}
## Task

A technician is requesting a step-by-step repair or diagnostic procedure.
Using ONLY the context above, answer their question.

Technician query: {{ query }}

## Output Format

You MUST respond with valid JSON ONLY — no markdown, no explanations outside
the JSON structure.  Follow this exact schema:

{{ diagnostic_response_schema }}

Ensure:
- "problem_summary" describes what the procedure accomplishes
- "possible_causes" lists conditions that warrant this procedure
- "inspection_steps" is a numbered sequence of actions in chronological order
- "recommended_actions" covers tools, safety precautions, and verification steps
- "referenced_entities" includes components, connectors, and special tools
- "citations" maps each claim to a source
